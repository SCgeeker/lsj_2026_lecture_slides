---
title: "W1：課程導航與實踐準則"
subtitle: "基礎統計學 2026"
author: "慈濟大學"
date: "`r Sys.Date()`"
output:
  revealjs::revealjs_presentation:
    theme: simple
    highlight: pygments
    center: false
    transition: slide
    self_contained: true
    css: custom.css
    reveal_options:
      slideNumber: true
      previewLinks: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE,
                      fig.width = 9, fig.height = 5)
library(ggplot2)
```

# 歡迎來到基礎統計學

---

## 統計學是什麼？

**不是**數學計算，而是——

> 在混亂的數據中，找到通往真相的地圖。

**核心問題**：

- 我們每天都在做判斷——但憑的是直覺還是證據？
- 「冰淇淋銷量越高，溺水人數越多」→ 吃冰淇淋會溺水嗎？
- 「考前拜拜的同學成績比較好」→ 拜拜能提高分數嗎？

---

## 直覺的陷阱

```{r illusion, fig.height=3.5}
# Müller-Lyer 錯覺：兩條等長線段看起來不一樣長
df_lines <- data.frame(
  x_start = c(2, 2),
  x_end   = c(8, 8),
  y       = c(2, 4),
  group   = c("A", "B")
)

# 箭頭端點
df_arrows <- data.frame(
  x = c(2, 2, 8, 8, 2, 2, 8, 8),
  y = c(2, 2, 2, 2, 4, 4, 4, 4),
  xend = c(1.2, 1.2, 8.8, 8.8, 2.8, 2.8, 7.2, 7.2),
  yend = c(2.5, 1.5, 2.5, 1.5, 4.5, 3.5, 4.5, 3.5)
)

ggplot() +
  geom_segment(data = df_lines,
               aes(x = x_start, xend = x_end, y = y, yend = y),
               linewidth = 2, color = "#2c3e50") +
  geom_segment(data = df_arrows,
               aes(x = x, xend = xend, y = y, yend = yend),
               linewidth = 1.2, color = "#e74c3c") +
  annotate("text", x = 5, y = 1, label = "兩條線一樣長嗎？",
           size = 6, color = "#2c3e50", fontface = "bold") +
  coord_fixed(ratio = 0.5) +
  theme_void() +
  theme(plot.margin = margin(10, 10, 10, 10))
```

> 連眼睛都會被騙——所以我們需要統計工具來輔助判斷。

---

## 偽相關：數據會說謊

| 變項 A | 變項 B | 真正原因 |
|:-------|:-------|:---------|
| 冰淇淋銷量 ↑ | 溺水人數 ↑ | **氣溫** 同時影響兩者 |
| 有機食品銷量 ↑ | 自閉症確診數 ↑ | **時代趨勢** 的巧合 |
| 尼可拉斯乘車電影數量 | 泳池溺水人數 | **純粹巧合** |

> 相關 ≠ 因果——這是本學期最重要的一句話。

**推薦網站**：[Spurious Correlations](https://www.tylervigen.com/spurious-correlations)


# 課程全貌預覽

---

## 學期四部曲

```{r roadmap, fig.height=4.5}
phases <- data.frame(
  phase = factor(c("Phase 1\n工具化", "Phase 2\n視覺化",
                    "Phase 3\n統計推論理論", "Phase 4\n統計推論實作"),
                 levels = c("Phase 1\n工具化", "Phase 2\n視覺化",
                            "Phase 3\n統計推論理論", "Phase 4\n統計推論實作")),
  weeks = c("W2-W4", "W5-W6", "W7-W11", "W12-W17"),
  desc = c("建立 SOP\n匯入·清理·設定", "學會「看」數據\n圖表·分佈·偏態",
           "學會「判斷」\n機率·CI·假設檢定", "學會「表達」\n t·χ²·r·迴歸"),
  y = 1,
  x = 1:4,
  color = c("#3498db", "#2ecc71", "#e74c3c", "#9b59b6")
)

ggplot(phases, aes(x = x, y = y)) +
  geom_tile(width = 0.9, height = 0.6,
            fill = phases$color, alpha = 0.2,
            color = phases$color, linewidth = 1.5) +
  geom_text(aes(label = phase), y = 1.18, size = 4.5, fontface = "bold") +
  geom_text(aes(label = weeks), y = 1.02, size = 3.5, color = "#555555") +
  geom_text(aes(label = desc), y = 0.82, size = 3, color = "#333333") +
  # 箭頭連接
  annotate("segment", x = 1.5, xend = 1.9, y = 1, yend = 1,
           arrow = arrow(length = unit(0.2, "cm")), color = "#95a5a6", linewidth = 1) +
  annotate("segment", x = 2.5, xend = 2.9, y = 1, yend = 1,
           arrow = arrow(length = unit(0.2, "cm")), color = "#95a5a6", linewidth = 1) +
  annotate("segment", x = 3.5, xend = 3.9, y = 1, yend = 1,
           arrow = arrow(length = unit(0.2, "cm")), color = "#95a5a6", linewidth = 1) +
  coord_cartesian(ylim = c(0.5, 1.5)) +
  theme_void() +
  labs(title = "基礎統計學 2026：學習旅程") +
  theme(plot.title = element_text(hjust = 0.5, face = "bold", size = 16),
        plot.margin = margin(10, 20, 10, 20))
```

---

## 各階段內容

| 階段 | 週次 | 關鍵技能 | 里程碑 |
|:-----|:-----|:---------|:-------|
| **工具化** | W2-W4 | 匯入·變項設定·資料清理 | 能獨立處理一份 CSV |
| **視覺化** | W5-W6 | 描述統計·圖表·偏態 | 能用圖表說故事 |
| **理論** | W7-W11 | 常態性·CLT·CI·假設檢定 | 理解 p 值的意義 |
| **實作** | W12-W17 | t 檢定·卡方·相關·迴歸 | 獨立完成分析報告 |

> W7-W11 是最抽象的「大魔王」區段——別擔心，我們有模擬工具來幫助理解。

---

## 對應教材

- 《用 jamovi 上手統計學》（免費線上電子書）
  - [中文版](https://scgeeker.github.io/lsj-book-zh_tw/)
  - [英文原版 PDF](lsj-free.pdf)

- [jamovi 社群資源](https://www.jamovi.org/community.html)

> 本課程所有教學簡報都附上對應的電子書章節連結。


# 為什麼選 jamovi？

---

## jamovi 的特色

:::::::::::::: {.columns}
::: {.column width="50%"}

**對學生友善**：

- 免費、開源
- 圖形介面，不需寫程式
- 即時顯示分析結果
- 內建學習資料集

:::
::: {.column width="50%"}

**對教學友善**：

- .omv 檔案記錄完整操作歷程
- 分析可再現（Reproducibility）
- 跨平台（Windows/Mac/Linux/雲端）
- R 語言為底層引擎

:::
::::::::::::::

---

## 分析即存檔

**jamovi 的核心概念**：

```
打開 CSV → 設定變項 → 執行分析 → 全部存在一個 .omv 檔案裡
```

- 不像 Excel——分析結果不會「消失」
- 不像 SPSS——不需要額外的語法檔
- **一個 .omv = 你的完整分析日記**

> AI 評量系統會讀取你的 .omv 檔案，確認你的每一步操作。


# 安裝 jamovi

---

## 方式一：桌面版安裝（推薦）

**下載網址**：[https://www.jamovi.org/download.html](https://www.jamovi.org/download.html)

| 平台 | 安裝方式 |
|:-----|:---------|
| **Windows** | 下載 `.exe` 安裝檔 → 雙擊安裝 |
| **macOS** | 下載 `.dmg` → 拖曳至 Applications |
| **Linux** | `flatpak install flathub org.jamovi.jamovi` |
| **ChromeOS** | 啟用 Linux (Crostini) → 同 Linux 安裝 |

---

**版本選擇**：

- **solid（穩定版）**：2.6.x — 適合作業繳交
- **current（最新版）**：2.7.x — 新增 Plots Tab 等功能

> 建議安裝 **2.7.x**，確保與課程教材一致。

---

## 方式二：jamovi Cloud（免安裝）

**網址**：[https://cloud.jamovi.org](https://cloud.jamovi.org)

:::::::::::::: {.columns}
::: {.column width="50%"}

**優點**：

- 瀏覽器直接使用
- 不佔硬碟空間
- 支援 iPad / Chromebook
- 永遠是最新版本

:::
::: {.column width="50%"}

**方案**：

| 方案 | 費用 |
|:-----|:-----|
| Guest | 免費（有限制） |
| Priority | USD $5/月 <br> 教育用團購|

[Priority訂閱登記]()

:::
::::::::::::::

**注意**：Cloud 版適合無法安裝軟體的同學，但**建議以桌面版為主**。

---

## 桌面版 vs 雲端版

| 比較項目 | 桌面版 | 雲端版 |
|:---------|:-------|:-------|
| 安裝 | 需下載安裝 | 瀏覽器開啟 |
| 離線使用 | ✓ | ✗ |
| 資料存放 | 本機 | 伺服器（用後刪除） |
| 版本 | 自行選擇 | 永遠最新 |
| 適用裝置 | 電腦 | 任何有瀏覽器的裝置 |
| **課程建議** | **主要使用** | 只能在電腦教室操作 |

---

## 安裝 lsj-data 模組

安裝完 jamovi 後，請安裝教科書資料模組(Cloud版已安裝)：

**步驟**：

1. 開啟 jamovi
2. 點擊右上角 **「+」** 圖示（Modules）
3. 選擇 **jamovi library**
4. 切換到 **Available** 頁籤
5. 找到 **lsj-data** → 點擊 **INSTALL**

---

**驗證安裝成功**：

- 左上角 ☰ → **Open** → **Data Library**
- 應出現 **learning statistics with jamovi** 資料夾
- 裡面包含 `booksales`、`clinicaltrial` 等教學資料集

> 下週（W2）會用到 `booksales` 資料，請確認安裝成功。


# 課程評量制度

---

## 雙檔案制度

每週作業（W3 起）需繳交**兩個檔案**：

:::::::::::::: {.columns}
::: {.column width="50%"}

**1. jamovi 分析檔**

`學號_姓名_W##_Lab.omv`

- 記錄你的**操作過程**
- AI 會檢查：
  - 變項類型是否正確
  - 是否使用 Filter
  - 分析設定是否完整

:::
::: {.column width="50%"}

**2. Word 分析報告**

`學號_姓名_W##_Report.docx`

- 記錄你的**思考過程**
- 教師會評閱：
  - 操作步驟說明
  - 分析結果呈現
  - 反思問題回答

:::
::::::::::::::

---

## 評量配分

| 評分項目 | 配分 | 說明 |評比方式|
|:---------|:----:|:-----|:-----|
| 格式與命名 | 15% | 檔名正確、結構完整 |自動|
| 分析結果呈現 | 25% | 正確貼上 jamovi 輸出表格 |自動|
| 操作紀錄 / 修正說明 | 30% | 記錄操作步驟與判斷理由 |手動|
| 反思問題回答 | 30% | 展現對統計概念的理解 |手動|

- 此表為原則，視各週作業問題安排調整

---

## 回饋與修正機制

```
繳交 → AI 評量 → 個人回饋報告 → 修正重繳 → 最終成績
```

1. **繳交**：每週上課結束前上傳至 Google Classroom
2. **評量**：2 天內收到個人化 HTML 回饋報告
3. **修正**：依據回饋改進，下次上課前重新繳交(另有安排輔導課)
4. **最終成績**：整合初次與修正成績

> 除 W17 期末外，每週都有**修正機會**——重點是學會，不是一次考滿分。


# 檔案命名規範

---

## 命名格式（非常重要！）

```
學號_姓名_W##_Lab.omv
學號_姓名_W##_Report.docx
```

**正確範例**：

- `113516001_王小明_W2_Lab.omv` ✓
- `113516001_王小明_W2_Report.docx` ✓

**錯誤範例**：

- `王小明作業.omv` ✗（缺學號、週次）
- `113516001 王小明 W2 Lab.omv` ✗（使用空格）
- `W2_Lab.omv` ✗（缺學號姓名）

> AI 程式系統只依靠檔名識別你是誰。**格式錯誤 = 直接扣分。**

---

## 數據完整性規範

**原始數據是神聖不可侵犯的。**

:::::::::::::: {.columns}
::: {.column width="50%"}

**禁止**：

- ❌ 在 Excel 中手動刪改數據後才匯入
- ❌ 直接刪除 jamovi 試算表中的列
- ❌ 在 Word 中手動修改統計數值

:::
::: {.column width="50%"}

**必須**：

- ✅ 使用 jamovi 的 **Filters** 排除異常值
- ✅ 使用 **Compute** 建立新變項
- ✅ 所有修改留下操作痕跡

:::
::::::::::::::

> AI 程式會掃描你的 .omv 檔案，確認操作的合規性。


# APA 寫作標準預覽

---

## 統計結果的標準寫法

本學期會逐步學習 **APA 格式**的統計報告寫法：

**範例**：

> 獨立樣本 t 檢定顯示，實驗組的焦慮分數顯著低於控制組，*t*(24) = -2.56, *p* = .017, *d* = -0.50。

**格式要點**：

| 元素 | 規則 | 範例 |
|:-----|:-----|:-----|
| 統計符號 | 斜體 | *t*, *p*, *r*, *F* |
| p 值 | 小數點前不加 0 | *p* = .017（非 0.017） |
| 效果量 | 必須報告 | *d* = -0.50 |

> 不用現在記住——每週作業都會練習。


# 實作示範

---

## Hello World：第一次開啟 jamovi

**請跟著做**：

1. 開啟 jamovi（桌面版或雲端版）
2. 觀察介面：
   - 左側 = 資料表（Data）
   - 右側 = 分析結果（Results）
   - 上方 = 分析選單（Analyses）
3. 左上角 ☰ → **Open** → **Data Library**
4. 選擇 **learning statistics with jamovi** → **booksales**
5. 觀察三個變項：Month、Days、Sales

---

## 第一次存檔

**步驟**：

1. 左上角 ☰ → **Save As**
2. 選擇存檔位置（建議建立專用資料夾）
3. 檔名輸入：`你的學號_你的姓名_W1_Lab.omv`
4. 點擊儲存

**檢核清單**：

- [ ] jamovi 已安裝（或可開啟 Cloud 版）
- [ ] lsj-data 模組已安裝
- [ ] 成功開啟 booksales 資料集
- [ ] 檔案已儲存且命名正確

---

## 本週繳交

**繳交方式**：上傳至 Google Classroom

**繳交項目**：`學號_姓名_W1_Lab.omv`

**檢核**：

1. ✅ 包含 booksales 資料
2. ✅ 檔名格式正確
3. ✅ 能正常開啟

> 這是最簡單的一次作業——目的是確認環境設定完成。


# 小結與展望

---

## 本週重點回顧

1. **統計的本質**：用證據取代直覺
2. **課程四部曲**：工具化 → 視覺化 → 理論 → 實作
3. **jamovi 安裝**：桌面版（推薦）或雲端版
4. **雙檔案制度**：.omv（過程）+ .docx（溝通）
5. **檔案命名**：`學號_姓名_W##_任務.副檔名`
6. **數據倫理**：原始數據不可直接修改

---

## 下週預告

**W2：與 jamovi 的第一次接觸**

- 匯入 CSV 檔案
- 認識測量尺度（Nominal / Ordinal / Continuous）
- 設定變項類型與標籤
- 建立計算變項（Compute）

**請先完成**：

1. 安裝 jamovi + lsj-data 模組
2. 繳交 W1 作業（`.omv` 檔案）

---

## 課程資源

| 資源 | 連結 |
|:-----|:-----|
| 中文電子書 | [用 jamovi 上手統計學](https://scgeeker.github.io/lsj-book-zh_tw/) |
| jamovi 下載 | [jamovi.org/download](https://www.jamovi.org/download.html) |
| jamovi Cloud | [cloud.jamovi.org](https://cloud.jamovi.org) |
| Google Classroom | 課程代碼：`edhbksfw` |
| 偽相關網站 | [tylervigen.com](https://www.tylervigen.com/spurious-correlations) |

---

## Q & A
